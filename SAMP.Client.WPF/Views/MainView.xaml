<UserControl x:Class="SAMP.Client.WPF.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">   
    <Grid Height="700" Width="525">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="0" x:Name="Name" Text="{Binding Username}" MaxLines="1" />
        
        <ListView Grid.Row="1" x:Name="SelectServer" ItemsSource="{Binding Servers}" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name"
                                    DisplayMemberBinding="{Binding Name}"
                                    Width="250" />
                    
                    <GridViewColumn Header="Hostname"
                                    DisplayMemberBinding="{Binding Hostname}"
                                    Width="120" />
                    
                    <GridViewColumn Header="Port"
                                    DisplayMemberBinding="{Binding Port}" 
                                    Width="50" />
                </GridView>
            </ListView.View>
        </ListView>
        
        <Button Grid.Row="2" x:Name="ShowAllServers" Content="Show All Servers"></Button>
        
    </Grid>
</UserControl>
